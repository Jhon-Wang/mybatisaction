<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.mybatis.example.Dao.BlogMapper">


    <select id="selectBlog" resultType="org.mybatis.example.entity.Blog">
        select * from blog where id = #{id}
    </select>

    <insert id="insertBlog" parameterType="org.mybatis.example.entity.Blog">
        INSERT INTO blog (id,content,auth) value (#{blog}.id,#{blog}.content,#{blog}.auth)
    </insert>


</mapper>